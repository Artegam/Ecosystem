#include "ScreenManager.h"

using namespace ScreenManager;

void MenuFunction::execute () {
  //TODO: ecrire ici la fonction de base pour un menu
  // par exemple comme pour les options dans une liste
  // mettre une croix (classe a renommer en optionMenu ?

  /*
  string suffix = name.substr(name.size() - 4);
  if (this->selected) {
    name = "[x] " + suffix;
  } else {
    name = "[ ] " + suffix;
  }
  */
}

